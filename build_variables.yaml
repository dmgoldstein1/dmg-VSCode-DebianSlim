# Example usage:
tunnel_name: 09042025test1
# force_analysis: true
# logging: true
# copilot_is_autonomous: true
git_user_name: "dmgoldstein1"
git_user_email: "dmgoldstein1@gmail.com"
# gh_token: "ghp_..."
auto_update_on_start: yes
update_check_interval_seconds: 1800
# approval_file: "/home/devuser/MY_APPROVAL_FILE"

# ------------------------------------------------------------------------------
# Supported build variables and their default values
# (Uncomment and set as needed. If not set, defaults below are used.)
# ------------------------------------------------------------------------------
# tunnel_name: ""                # (string) Optional. Default: "" (auto-generated)
# force_analysis: false          # (bool)   Default: false
# force_rebuild: false           # (bool)   Default: false
# logging: false                 # (bool)   Default: false
# copilot_is_autonomous: false   # (bool)   Default: false
# git_user_name: ""              # (string) Optional. Default: ""
# git_user_email: ""             # (string) Optional. Default: ""
# gh_token: ""                   # (string) Optional. Default: ""
# auto_update_on_start: true     # (bool)   Default: true
# update_check_interval_seconds: 3600 # (int) Default: 3600
# approval_file: "$HOME/APPROVE_CODE_UPDATE" # (string) Default: "$HOME/APPROVE_CODE_UPDATE"
# Generated by Copilot (GPT-4)
# YAML configuration for build.sh when invoked with --use-yaml.
# Description: Provide values here to avoid long command lines. The build script will ignore
# all CLI flags/args when --use-yaml is present and instead read keys from this file.
#
# Security note: Do NOT commit real secrets (e.g., gh_token) to version control.
# Prefer sourcing sensitive values from your environment or a secure secret manager.
# If you must edit gh_token here for a one-off run, be sure to remove it before committing.
#
# How to use:
#   ./build.sh --use-yaml
#
# Supported keys (all optional). Leave commented or blank to skip.
#
# tunnel_name: Name for the VS Code Tunnel and the container name (if provided).
# - Allowed characters: lowercase letters, numbers, and underscores (^[a-z0-9_]+$)
#
# force_analysis: Whether to force re-running Docker Scout analysis even if logs already exist.
# - boolean: true | false
#
# force_rebuild: Whether to force rebuilding the Docker image even if it already exists.
# - boolean: true | false
#
# logging: Enable verbose logging output in the build script.
# - boolean: true | false
#
# copilot_is_autonomous: Configure the running container to auto-approve Copilot terminal commands.
# - boolean: true | false
#
# git_user_name: Git user.name to be configured inside the container.
# git_user_email: Git user.email to be configured inside the container.
#
# gh_token: GitHub token used by the container's entrypoint for non-interactive gh auth login.
# - WARNING: Avoid committing this. Prefer to pass it at runtime via environment or edit locally and remove.
#
# auto_update_on_start: If set, controls whether the container auto-installs VS Code CLI updates at startup.
# - boolean-like: true | false (string values are accepted by the entrypoint)
#
# update_check_interval_seconds: Frequency for mid-run update checks by the entrypoint watcher.
# - integer seconds (e.g., 3600)
#
# approval_file: Path inside the container that, when created, authorizes a mid-run update install.
# - string path (e.g., /home/devuser/APPROVE_CODE_UPDATE)
#
# port: Host port to expose the container on (default: auto-find starting at 8080)
# - integer (e.g., 3000)
#
# image_name: Docker image name (default: dmg-vs-code-debian-slim)
# - string (e.g., my-custom-vscode)
#
# container_name: Container name (default: based on tunnel_name or vscode-server-tunnel)
# - string (e.g., my-custom-container)
#
# ----------------------
# Example configuration:
#
# tunnel_name: my_team_tunnel_01
# force_analysis: false
# force_rebuild: false
# logging: true
# copilot_is_autonomous: false
# git_user_name: "Your Name"
# git_user_email: "you@example.com"
# gh_token: "ghp_example_PLACEHOLDER_do_not_commit"
# auto_update_on_start: true
# update_check_interval_seconds: 3600
# approval_file: "/home/devuser/APPROVE_CODE_UPDATE"
# port: 3000
# image_name: my-custom-vscode
# container_name: my-custom-container
